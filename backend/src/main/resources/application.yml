# 主配置文件
server:
  port: ${SERVER_PORT:8000}

spring:
  profiles:
    active: ${SPRING_PROFILES_ACTIVE:docker}

# JWT配置
jwt:
  secret: ${JWT_SECRET:mySecretKeyForJwtTokenGeneration2024ExamSystemVeryLongKeyString}
  expiration: 86400000
